from setuptools import setup, find_packages

setup(
    name='utilsbox',
    version='0.0.0',
    description='',
    package_dir={"": "src"},
    packages=find_packages("src"),
    install_requires=[
        
    ],
)